import React from "react";
import { View, Text } from "react-native";

const StripeSuccessScreen = () => {
    return (
        <View>
            <Text>🎉 Your Stripe account has been successfully connected!</Text>
        </View>
    );
};

export default StripeSuccessScreen;
