import React from 'react';
import { View, Text } from 'react-native';

const NotFoundPage = () => {
    return (
        <View>
            <Text>404</Text>
        </View>
    );
};

export default NotFoundPage;